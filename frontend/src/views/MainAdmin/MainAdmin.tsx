import React from "react";

interface Props {
  title: string;
}

export const MainAdmin: React.FC<Props> = (props) => {
  return <div>This is the the admin page</div>;
};

export default MainAdmin;
