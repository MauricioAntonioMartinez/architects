query Job($id: String!) {
  getJob(jobId: $id) {
    id
    salary
    job
    nomina
    cratedAt
    observations
    isActive
    employee {
      id
      name
      heal_plan
      phone
      INE
      address
    }
    building {
      id
      name
    }
  }
}
